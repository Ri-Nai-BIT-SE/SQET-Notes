# 💡 复习建议与避坑指南

## 复习重点

对于白盒测试，**不要只看概念**，一定要找一道例题，亲手画一下**控制流图**，算一下**圈复杂度**。这是这门课最容易拿满分，也最容易因为手生而扣分的地方。

## 复习避坑指南（老师原话总结）

### 1. 大题图画对

**关键**：如果考基本路径测试，**控制流图**画错了，后面的圈复杂度和路径全错，10分直接扣光。

**注意事项**：
- 必须画**控制流图**（圆圈+箭头），不要画成程序流程图（方框+菱形）
- **汇聚节点**必须画出来（if语句结束后的汇合点）
- **复合条件**必须拆分（如 `if(A && B)` 要拆成两个判断节点）

### 2. 区分图形

- **程序流程图**：方框（处理）+ 菱形（判断）
- **控制流图**：圆圈（节点）+ 箭头（边）
- **基本路径法要画控制流图**，不要混淆

### 3. 覆盖率陷阱

**常见错误**：
- 题目问"最小测试用例数"，不要想当然填1234，要根据逻辑算
- 条件组合覆盖：n个条件需要 2^n 个用例
- **不要迷信"路径覆盖"**：如果有循环，路径覆盖通常达不到100%

**判断题陷阱**：
- "满足条件覆盖，不一定满足判定覆盖" → **对**（这是常见陷阱）

### 4. 题型变化

**趋势**：
- **以前**：主观题（让你写全过程）
- **现在**：**填空化**（给出现成的图或表，让你填圈复杂度是几，路径是哪几条）

**应对策略**：
- 这种题**"只认结果"**，算错一个数就没分
- 必须用三个公式互相验证圈复杂度
- 路径必须严格按照定义（至少包含一条新边）

### 5. 圈复杂度计算

**三个公式必须一致**：
1. V(G) = 区域数（包括外部区域）
2. V(G) = E - N + 2
3. V(G) = P + 1

**如果三个公式结果不一致，说明图有错误，必须重新检查！**

## 重点练习内容

建议重点练习：
1. **PPT P103**：代码找错（静态白盒测试）
2. **PPT P9**：边界值（结合黑盒测试）
3. **基本路径测试的计算流程**：画图 → 算圈复杂度 → 找路径 → 设计用例

## 考试时间分配建议

- **基本路径测试大题**：15-20分钟（必须仔细，一步错全错）
- **逻辑覆盖选择题**：2-3分钟/题
- **代码审查题**：5-10分钟

> **来源文件**：PPT，`output_4_15_1.txt`，`output_4_15_3.txt`
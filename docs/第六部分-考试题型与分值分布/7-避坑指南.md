# 5. 避坑指南（老师反复提及的扣分点）

### 🛑 测试用例设计规范

1. **测试用例必须包含三要素**（缺一不可，否则扣分）：
   - **ID（编号）**：TC_01, TC_02, ... 或 用例1, 用例2, ...
   - **输入数据**：明确列出所有输入值
   - **预期输出**：必须写，不要只写输入数据

2. **等价类划分规范**：
   - 必须列出**有效等价类**和**无效等价类**，并编号
   - 设计用例时，覆盖无效等价类要**一条用例只覆盖一个**（防止屏蔽效应）
   - 覆盖有效等价类时，尽可能多覆盖（合并测试）

3. **控制流图规范**：
   - 必须有汇聚节点
   - 复合条件必须拆分
   - 箭头方向不要画反

### 🛑 选择题陷阱

1. **注意题目问法**：
   - "正确的是" vs "错误的是"
   - "最佳答案"（单选题，选最合适的）

2. **常见陷阱**：
   - "测试是为了证明程序没有错误" -> **错**（Myers观点：测试是为了发现错误）
   - "满足条件覆盖一定满足判定覆盖" -> **错**（这是判断题常见陷阱）
   - "自动化测试能完全取代人工测试" -> **错**

### 🛑 计算题注意事项

1. **覆盖率计算**：
   - 理解原理，不要死记硬背
   - 路径覆盖：如果有循环，通常无法达到100%

2. **最小测试用例数计算**：
   - **4n+1原则**：n个变量的边界值测试
   - **2^n**：条件组合覆盖（n个条件）
   - 理解原理，灵活应用

3. **圈复杂度计算**：
   - 必须用三个公式互相验算
   - 结果一致说明图画对了
   - 如果画错了控制流图，后面的分全丢

### 🛑 大题答题技巧

1. **黑盒测试大题**：
   - 必须画等价类表（输入条件、有效等价类、无效等价类，并编号）
   - 用例编号必须对应清楚
   - 注意隐含条件（如三角形问题的数学条件）

2. **白盒测试大题**：
   - 控制流图必须画对（如果画错，后面全丢分）
   - 圈复杂度必须算对（用三个公式验证）
   - 独立路径必须找全（数量 = 圈复杂度）

3. **案例分析题**：
   - 即使不会，也要根据图表数据写术语
   - 多写专业术语：响应时间、吞吐量、资源利用率、瓶颈、内存泄漏等
   - 看着哪根线飙高了就说哪里有瓶颈

> **来源文件**：`output_4_13_3.txt`，`output_4_14_1.txt`，`output_4_14_3.txt`，`output_4_15_1.txt`，`output_4_15_3.txt`
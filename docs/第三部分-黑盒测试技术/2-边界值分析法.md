# 边界值分析法（Boundary Value Analysis）

录音中老师强调：**"错误往往隐藏在角落里（边界上）"**。它是等价类的补充。

### 基本原理

**核心假设**：错误更可能出现在输入变量的极值附近（**单故障假设**）。

### 📍 边界值的选择策略

#### 1. 标准边界值测试（5点法）

**课件标准**：边界值分析的5个测试点为：
- **min**（最小值）
- **min+**（略大于最小值）
- **mid/nom**（正常值/中间值）
- **max-**（略小于最大值）
- **max**（最大值）

假设输入范围是 `[1, 100]`（整数）：

**5个测试点**：
1. **min（最小值）**：1 （有效边界）
2. **min+（略大于最小值）**：2 （有效边界附近）
3. **mid/nom（正常值）**：50 （有效正常值）
4. **max-（略小于最大值）**：99 （有效边界附近）
5. **max（最大值）**：100 （有效边界）

**老师原话**（录音）：
> "输入变量的最小值、略大于最小值、输入值域内的任意值、略小于最大值和最大值来设计测试用例。"

**注意**：标准5点法通常不包含无效边界（min- 和 max+），只关注有效范围内的边界。

#### 2. 健壮性测试（7点法）

在5点法的基础上，增加两个无效边界点，重点关注**预期的输出**（异常处理）。

**7个测试点**：
1. **略小于最小值 (min-)**：0 （无效，测试健壮性）
2. **最小值 (min)**：1 （有效边界）
3. **略大于最小值 (min+)**：2
4. **正常值 (nom)**：50
5. **略小于最大值 (max-)**：99
6. **最大值 (max)**：100 （有效边界）
7. **略大于最大值 (max+)**：101 （无效，测试健壮性）

### 📊 测试用例数量公式（考点）

#### 标准边界值测试（5点法）

对于 **n 个变量**的程序，采用标准边界值分析（5点法），最少产生 **4n + 1** 个测试用例。

**公式解释**（课件标准）：
- 保留其中一个变量，让其余的变量取正常值
- 被保留的变量依次取：**min, min+, mid, max-, max**（5个值）
- 但根据**4n+1公式**，实际只取其中4个边界值（min, min+, max-, max），加上1个所有变量都取正常值的用例
- 每个变量取4个边界值（min, min+, max-, max），其余变量取正常值 → 4n 个用例
- 所有变量都取正常值 → 1 个用例
- **总计**：4n + 1 个用例

**老师原话**（录音）：
> "保留其中一个变量，让其余的变量取正常值，被保留的变量依次是最小、比最小稍微大一点、正常值、比最大稍微小一点、最大值。"
> "对于一个含有N个变量的程序，边界值分析最少产生4N+1个测试用例。"

**示例**：2个变量 (x, y)，范围都是 [1, 100]
- 用例数 = 4×2 + 1 = 9 个用例

#### 最坏情况测试（拒绝单故障假设）

**核心思想**：拒绝"单故障"假设，考虑多个变量同时取极值的情况。

**计算方法**：使用 **笛卡尔积** 计算用例数。

**公式**：如果每个变量取 k 个值，n 个变量，用例数为 **k^n**

**示例**：
- 2个变量，每个取5个值（5点法）→ 用例数 = 5² = 25 个
- 3个变量，每个取5个值 → 用例数 = 5³ = 125 个

**适用场景**：当怀疑多个变量同时取极值可能引发问题时。

### 常见边界值（实际应用）

1. **整数边界**：
   - 16-bit 整数：32767 (最大值), -32768 (最小值)
   - 32-bit 整数：2147483647, -2147483648

2. **数组边界**：
   - 第一个元素：索引 0
   - 最后一个元素：索引 n-1
   - 空数组：长度为 0

3. **循环边界**：
   - 第0次（循环前）
   - 第1次（首次执行）
   - 倒数第2次
   - 最后一次

4. **字符串边界**：
   - 空字符串：长度为 0
   - 单个字符：长度为 1
   - 最大长度

### 示例

如果输入范围是 [1, 100]，则选取：

**标准5点法**：
- 1（最小值）
- 2（略大于最小值）
- 50（正常值）
- 99（略小于最大值）
- 100（最大值）

**健壮性7点法**：
- 0（略小于最小值，无效）
- 1（最小值）
- 2（略大于最小值）
- 50（正常值）
- 99（略小于最大值）
- 100（最大值）
- 101（略大于最大值，无效）

**考试注意**：
- 如果题目提到**4n+1**原则，指的是标准边界值测试（5点法）
- 如果涉及到**有序集合**（如列表），要测第一个元素和最后一个元素
- 区分标准边界值测试、健壮性测试（7点）、最坏情况测试（笛卡尔积）

> **来源文件**：`output_4_12_3.txt`，`output_4_13_1.txt`
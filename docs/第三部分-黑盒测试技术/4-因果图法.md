# 因果图法（Cause-Effect Graphing）

### 为什么要使用因果图法？（背景）

在**等价类划分法**和**边界值分析法**中，我们主要关注的是**输入条件本身**（例如输入的范围、类型等），而往往忽略了以下两个关键点：

1. **输入条件的组合**：在实际软件中，很多功能是由多个输入条件共同决定的。
   - 例如："只有当输入 A 有效 **且** 输入 B 无效时，程序才报错"

2. **输入之间的制约关系**：某些输入条件之间存在逻辑约束。
   - 例如："输入 A 和输入 B 不能同时存在"

**因果图法**正是为了解决这个问题而诞生的。它是一种系统性的方法，用于选择高效的测试用例，以此来检查**输入条件的各种组合**。

### 核心思想

因果图法的核心思路可以概括为：**从自然语言描述中找出"因"和"果"，通过画图，最终转换为判定表。**

**转化过程**：
```
自然语言规格说明 → 形式语言规格说明（因果图） → 判定表 → 测试用例
```

**核心概念**：
- **因 (Cause)**：即 **输入条件** 或 输入条件的等价类
- **果 (Effect)**：即 **输出结果** 或 程序状态的改变

**优势**：
- 不仅能设计出高质量的测试用例
- 还能帮助我们发现规格说明书中可能存在的**不完整性**和**二义性**

### 适用场景

当题目中出现**"如果...并且...或者..."**这种复杂的逻辑组合时，单纯用等价类搞不定，必须用因果图。

适用于：
- 输入条件的组合逻辑复杂
- 输入条件之间存在约束关系
- 需要系统性地覆盖所有可能的组合

### 🧩 用因果图生成测试用例的步骤

这是一个标准化的流程：

1. **分析规格说明**：
   - 找出所有的**原因**（输入）
   - 找出所有的**结果**（输出）
   - 给每个原因和结果赋予一个**标识符**

2. **画出因果图**：
   - 分析语义，找出原因与结果之间、原因与原因之间的对应关系（逻辑关系）
   - 使用关系符号画图

3. **标记约束**：
   - 由于语法或环境限制，某些组合不可能出现
   - 使用约束符号在图上标明

4. **转换为判定表**：
   - 将因果图转化为**判定表**（Decision Table）
   - 考虑到约束，剔除不可能的组合

5. **设计测试用例**：
   - 把判定表的**每一列**拿出来作为依据，设计一个测试用例

### 符号系统（必考）

因果图使用特定的图形符号来表示逻辑关系。主要分为两类：

#### A. 关系符号（表示原因与结果之间的逻辑关系）

| 符号 | 含义 | 解释 |
| :--- | :--- | :--- |
| **恒等 (Identity)** | **—** | 若原因出现，则结果出现；若原因不出现，则结果也不出现。 |
| **非 (Not, ~)** | **~** | 若原因出现，则结果**不**出现；若原因不出现，则结果出现。 |
| **或 (Or, ∨)** | **∨** | 若几个原因中**有一个或更多**出现，则结果出现。 |
| **与 (And, ∧)** | **∧** | 若几个原因**都**出现，结果才出现。 |

**记忆方法**：
- **恒等**：原因和结果一致（原因=结果）
- **非**：原因和结果相反（原因≠结果）
- **或**：多个原因中**至少一个**成立 → 结果成立
- **与**：多个原因**全部**成立 → 结果成立

#### B. 约束符号（表示原因与原因之间、结果与结果之间的制约关系）

为了表示某些组合情况在实际中是不可能出现的（例如，一个人不能既是男性又是女性），我们需要引入约束符号。

| 约束类型 | 符号 | 含义 | 示例 |
| :--- | :--- | :--- | :--- |
| **E (Exclusive, 互斥)** | **E** | 表示 a, b 两个原因**不会同时成立**，最多有一个可能成立。 | 性别（男/女）不能同时为真 |
| **I (Inclusive, 包含)** | **I** | 表示 a, b, c 三个原因中**至少有一个**必须成立。 | 支付方式（现金/刷卡/支付宝），至少选一种 |
| **O (Only, 唯一)** | **O** | 表示 a 和 b 当中**必须有一个，且仅有一个**成立。 | 单选题，必须且只能选一个选项 |
| **R (Request, 要求)** | **R** | 表示当 a 出现时，b **必须**也出现。 | 选择了"会员"，必须填写"会员号" |
| **M (Mask, 屏蔽)** | **M** | （针对结果）表示当结果 a 是 1 时，结果 b **必须**是 0。 | 显示"成功"消息时，不能同时显示"失败"消息 |

**记忆方法**：
- **E (互斥)**：不能同时（Exclusive）
- **I (包含)**：至少一个（Inclusive）
- **O (唯一)**：必须且仅有一个（Only）
- **R (要求)**：一个出现，另一个必须出现（Request）
- **M (屏蔽)**：一个结果出现，另一个结果不能出现（Mask）

### 判定表结构（因果图转换后）

**格式说明**：因果图转换为判定表后，格式与判定表法相同。

| 条件桩 | 规则1 | 规则2 | 规则3 | ... |
|--------|-------|-------|-------|-----|
| 条件1  | Y/N   | Y/N   | Y/N   | ... |
| 条件2  | Y/N   | Y/N   | Y/N   | ... |
| **动作桩** |       |       |       |     |
| 动作1  | √     |       | √     | ... |
| 动作2  |       | √     |       | ... |

**转换过程**：
1. 因果图中的"因" → 判定表的"条件桩"
2. 因果图中的"果" → 判定表的"动作桩"
3. 根据因果图的逻辑关系，填入条件项和动作项
4. **考虑到约束，剔除不可能的组合**
5. 每一列（规则）生成一个测试用例

---

## 因果图法实例解析

### 实例1：薪金计算系统

**规则描述**：
- 年薪制员工：严重过失 → 扣年终风险金 4%；过失 → 扣年终风险金 2%
- 非年薪制员工：严重过失 → 扣当月薪资 8%；过失 → 扣当月薪资 4%

#### 分析步骤

**第一步：列出原因和结果**

**原因（Cause）**：
- C1: 年薪制员工
- C2: 非年薪制员工
- C3: 严重过失
- C4: 过失

**结果（Effect）**：
- A1: 扣风险金4%
- A2: 扣风险金2%
- A3: 扣月薪8%
- A4: 扣月薪4%

**第二步：画因果图**

**逻辑关系**：
- C1（年薪制）∧ C3（严重过失）→ A1（扣风险金4%）
- C1（年薪制）∧ C4（过失）→ A2（扣风险金2%）
- C2（非年薪制）∧ C3（严重过失）→ A3（扣月薪8%）
- C2（非年薪制）∧ C4（过失）→ A4（扣月薪4%）

**约束关系**：
- C1 和 C2 互斥（E）：不能既是年薪制又是非年薪制
- C3 和 C4 互斥（E）：不能既是严重过失又是过失

**第三步：生成判定表**

考虑到约束（C1和C2互斥，C3和C4互斥），剔除不可能的组合后，有效的规则有：

| 条件桩 | 规则1 | 规则2 | 规则3 | 规则4 |
|--------|-------|-------|-------|-------|
| C1: 年薪制? | Y | Y | N | N |
| C2: 非年薪制? | N | N | Y | Y |
| C3: 严重过失? | Y | N | Y | N |
| C4: 过失? | N | Y | N | Y |
| **动作桩** | | | | |
| A1: 扣风险金4% | √ | | | |
| A2: 扣风险金2% | | √ | | |
| A3: 扣月薪8% | | | √ | |
| A4: 扣月薪4% | | | | √ |

**第四步：生成测试用例**

- **TC1**：年薪制 + 严重过失 → 扣风险金4%
- **TC2**：年薪制 + 过失 → 扣风险金2%
- **TC3**：非年薪制 + 严重过失 → 扣月薪8%
- **TC4**：非年薪制 + 过失 → 扣月薪4%

### 实例2：自动售货机

**规则描述**：
- 处理单价1元5角的饮料
- 投入1元5角硬币 + 按下"可乐/雪碧/红茶"按钮 → 送出相应饮料
- 投入2元硬币 → 送出饮料 + 退还5角硬币

#### 分析步骤

**第一步：列出原因和结果**

**原因（Cause）**：
- C1: 投入1.5元
- C2: 投入2元
- C3: 按可乐按钮
- C4: 按雪碧按钮
- C5: 按红茶按钮

**中间状态**（可选）：
- 已投币
- 已按钮

**结果（Effect）**：
- A1: 退还5角
- A2: 送出可乐
- A3: 送出雪碧
- A4: 送出红茶

**第二步：画因果图**

**逻辑关系**：
- C1（投1.5元）∧ C3（按可乐）→ A2（送出可乐）
- C1（投1.5元）∧ C4（按雪碧）→ A3（送出雪碧）
- C1（投1.5元）∧ C5（按红茶）→ A4（送出红茶）
- C2（投2元）∧ C3（按可乐）→ A2（送出可乐）∧ A1（退还5角）
- C2（投2元）∧ C4（按雪碧）→ A3（送出雪碧）∧ A1（退还5角）
- C2（投2元）∧ C5（按红茶）→ A4（送出红茶）∧ A1（退还5角）

**约束关系**：
- C1 和 C2 互斥（E）：不能同时投1.5元和2元
- C3、C4、C5 互斥（E）：不能同时按多个按钮

**第三步：生成判定表**

考虑到约束，有效的规则有：

| 条件桩 | 规则1 | 规则2 | 规则3 | 规则4 | 规则5 | 规则6 |
|--------|-------|-------|-------|-------|-------|-------|
| C1: 投1.5元? | Y | Y | Y | N | N | N |
| C2: 投2元? | N | N | N | Y | Y | Y |
| C3: 按可乐? | Y | N | N | Y | N | N |
| C4: 按雪碧? | N | Y | N | N | Y | N |
| C5: 按红茶? | N | N | Y | N | N | Y |
| **动作桩** | | | | | | |
| A1: 退还5角 | | | | √ | √ | √ |
| A2: 送出可乐 | √ | | | √ | | |
| A3: 送出雪碧 | | √ | | | √ | |
| A4: 送出红茶 | | | √ | | | √ |

**第四步：生成测试用例**

- **TC1**：投1.5元 + 按可乐 → 送出可乐
- **TC2**：投1.5元 + 按雪碧 → 送出雪碧
- **TC3**：投1.5元 + 按红茶 → 送出红茶
- **TC4**：投2元 + 按可乐 → 送出可乐 + 退还5角
- **TC5**：投2元 + 按雪碧 → 送出雪碧 + 退还5角
- **TC6**：投2元 + 按红茶 → 送出红茶 + 退还5角

---

### 总结

因果图法通过图形化的方式，清晰地展示了输入与输出之间的复杂逻辑关系，特别是**组合逻辑**和**约束条件**。它弥补了等价类划分和边界值分析的不足，是设计高覆盖率、高质量测试用例的有力工具。

**关键要点**：
1. 因果图法适用于输入条件有复杂组合逻辑的场景
2. 必须识别并标记约束关系（互斥、包含等）
3. 最终生成的**判定表**是测试执行的直接依据
4. 判定表中每一列（规则）对应一个测试用例

> **来源文件**：`output_4_13_1.txt`，`output_4_14_1.txt`，`output_4_14_3.txt`

---